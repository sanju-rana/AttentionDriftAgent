# Dependencies for the local Attention Drift Agent (runs on your own machine,
# NOT inside the backend container).

requests==2.32.3
pynput==1.7.7

# --- Active window detection (OS-specific) ---
pywin32==306 ; sys_platform == "win32"
pyobjc-framework-Quartz==10.3.1 ; sys_platform == "darwin"
pyobjc-framework-Cocoa==10.3.1 ; sys_platform == "darwin"
psutil==6.0.0 ; sys_platform == "win32"
python-xlib==0.33 ; sys_platform == "linux"